package com.onee.rusty;

import com.facebook.react.bridge.ReadableArray;
import com.onee.rusty.glue.PropertyMap;

abstract public class RustReadableArray implements ReadableArray {
    public RustReadableArray(PropertyMap propertyMap) {
    }
}
